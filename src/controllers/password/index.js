const resetPassword = require('../password/resetPassword.controller');
const changePassword = require('../password/changePassword.controller');
const forgotPassword = require('../password/forgotPassword.controller');
module.exports = {
    changePassword,
    resetPassword,
    forgotPassword
};