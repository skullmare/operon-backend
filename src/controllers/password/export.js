const resetPassword = require('./reset');
const changePassword = require('./change');
const forgotPassword = require('./forgot');
module.exports = {
    changePassword,
    resetPassword,
    forgotPassword
};